# 钉钉自动开户与首次密码设置

本企业员工首次完成钉钉网页授权后，系统核验企业身份。没有已有账号或冲突档案时，自动建立普通成员账号、保存钉钉资料与绑定，然后立即显示“设置登录密码”。本人完成密码设置前不能进入工作台或访问业务接口；以后可以使用姓名和密码，也可以继续使用钉钉登录。

## 开启方式

沿用已经验证的应用凭据、身份信息权限及回调地址，增加：

```env
DINGTALK_LOGIN_ENABLED=true
DINGTALK_LOGIN_SCOPE=company
DINGTALK_AUTO_REGISTER_ENABLED=true
```

`company` 仅放开本企业已激活的内部成员，仍受钉钉应用可使用范围和接口权限约束，不接受外部联系人或其他企业账号。默认值保留 `pilot` 和自动开户关闭，支持原有试用模式。灰度继续使用独立 Cookie、数据库和端口；这项功能不依赖客户端 SDK。

## 账号与资料

- 新账号的姓名来自核验后的钉钉成员详情；优先使用企业邮箱 `org_email`，其次成员邮箱 `email`。没有邮箱也能开户和设置密码。
- 独立登录身份记录保存企业 Corp ID、userId、unionId，以及白名单内的姓名、邮箱、企业邮箱、手机号、头像 URL、工号和部门 ID。可选字段只有钉钉返回有效值时才保存；不保存访问令牌、应用密钥、授权码或未经筛选的完整响应。
- 自动建立本人钉钉通知绑定，来源为 `self_service`，并记录审计。开户本身不发送通知。
- 每次钉钉授权可更新保存的身份资料，但不覆盖已有系统账号的姓名、邮箱、角色或密码。
- 与已有账号同名、同邮箱或已有通知绑定时，不自动接管账号：已有密码的账号由本人验证原密码后关联；停用账号、没有密码的历史档案或身份冲突由管理员核对。保持历史缺陷和项目归属。
- 新账号固定为普通成员，不会因首次钉钉登录获得管理员权限。姓名密码登录及个人设置允许钉钉返回的非中文姓名；手工注册原有的中文姓名规则保持不变。

## 强制设置密码

`app_users.password_setup_required` 标记首次设置状态；无密码的钉钉账号同样按待设置处理。旧账号迁移时默认不强制重设已有密码。

待设置会话仅允许获取本人登录状态、提交首次密码和退出。业务接口以及其他认证操作返回 `PASSWORD_SETUP_REQUIRED`。前端在首次加载、刷新、普通登录、关联回调和接口拦截时都优先显示密码设置页；删除 URL 参数或返回旧页面不能跳过。

密码沿用系统的 6–128 个字符规则，需要重复输入确认，并以 scrypt 哈希保存。设置操作锁定账号与当前有效会话，完成后清除该账号全部旧会话并签发新会话。已经设置完成的账号不能重复调用此接口重置密码。管理员重置待设置账号的密码不会解除本人首次设置要求。

设置完成后恢复原缺陷或设置页链接。页面只提供设置密码与退出入口；测试及实际使用的密码均由账号本人输入，不应发到聊天中。

## 数据迁移与验收

增加 `app_users.password_setup_required` 和 `dingtalk_login_identities.profile` 两列，分别默认 `false` 和空对象。不会删除旧身份、密码、绑定、项目或缺陷。

```powershell
npm run build
npm test
```

`tests/dingtalk-onboarding.integration.test.ts` 默认跳过，仅在显式设置 `TRACEBUG_ONBOARDING_TEST=true`、专用本地测试连接为 `127.0.0.1:5434` 且无 `DATABASE_URL` 覆盖时运行，并创建独立临时数据库：

```powershell
$env:TRACEBUG_ONBOARDING_TEST = 'true'
npm run test:dingtalk-onboarding
Remove-Item Env:TRACEBUG_ONBOARDING_TEST
```

本分支的隔离测试助手位于忽略上传的 `分支资料_codex-dingtalk-message-integration/生成资料/run-isolated-auth-test.mjs`，可加 `--onboarding` 运行。它使用临时 PostgreSQL 和模拟钉钉身份，覆盖自动开户、资料白名单、权限拦截、强制设密、会话撤销、并发、同名或邮箱冲突、无邮箱账号及旧登录回归。页面验收脚本与截图保存在同一资料目录。

灰度真人验收：使用尚未注册且没有同名/邮箱冲突档案的本企业员工完成钉钉授权，确认立即进入密码设置页；本人设置后进入系统，退出，再使用页面展示的姓名与刚设置的密码登录。已有绑定账号应按原方式直接进入系统。

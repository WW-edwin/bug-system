# 钉钉通知规则

管理员在“后台设置 → 通知规则”配置所有项目共用的推送规则。也可以访问 `/?settings=notifications`，登录管理员账号后直接进入该页面。

## 可配置内容

每条规则包含名称、启用状态、触发事件和接收者。

| 触发事件 | 条件 | 可选接收者 |
| --- | --- | --- |
| 新建缺陷 | 缺陷创建成功 | 负责人、创建人，可同时勾选 |
| 状态流转 | 状态实际变更为指定目标状态 | 负责人、创建人，可同时勾选 |

目标状态直接来自“缺陷字典 → 状态”，使用稳定的字典值关联。修改状态名称后规则仍指向原状态；停用目标状态后，需要关闭对应规则或改选启用状态。新建默认状态沿用状态字典默认值，目前为“待处理”。

通知规则页与缺陷字典共用最新状态版本。字典新增或改名后，状态选项随页面数据刷新更新，不覆盖正在编辑的规则草稿；迟到的旧响应不会覆盖新状态。下拉框完整显示字典状态，停用项标注“已停用”并禁选。

字典值可以在“缺陷字典”中点击删除并保存，但被缺陷当前字段或通知规则引用时不能删除。若需删除某个规则目标状态，应先修改或删除关联规则；只停用规则仍会保留引用。历史活动和已排队消息使用原有快照，不因字典值删除而改写。

初次升级默认保留一条启用的“新建缺陷通知负责人”，保持此前的新建通知行为。可以新增“流转到待复测时通知创建人”等规则，也可以勾选两类接收者。最多 50 条规则，名称最多 80 字符，每条至少选择一类接收者。

修改后点击“保存通知规则”才生效。“取消修改”恢复上次保存内容；删除全部规则并保存或停用全部规则后，不再为新发生的事件排入通知。

## 触发和收件行为

- 单条修改、个人中心修改、批量修改状态使用同一套规则。重复保存相同状态不会触发；只改标题、描述、负责人等其他字段不会触发状态规则。
- 管理员可以修改所有缺陷的状态，包括非本人创建或负责的缺陷；其操作仍按上述规则发送通知，创建人仍为原始创建账号。缺陷权限见 [ISSUE_PERMISSIONS.md](ISSUE_PERMISSIONS.md)。
- 多人负责人全部纳入所选收件范围。创建人与负责人重合，或同一事件命中多条规则时，按系统用户去重，只向同一个人发送一次。
- 同一缺陷先进入目标状态、离开后再次进入，属于两次独立事件，可以分别通知。
- “创建人”使用缺陷原始创建账号，不等于本次修改人。状态通知包含原状态、新状态和修改人，并保留打开对应缺陷的网页链接。
- 通知标题和正文首行使用“项目名 · [优先级] Bug 编号”，项目名取自该缺陷所属项目，不再使用固定的“TraceBug 新缺陷”前缀。
- 规则在业务事务中匹配并形成事件及接收角色快照。保存规则只影响后续事件，不重新发送历史事件，也不改写已经排队的通知。
- 发送前复核用户是否启用、是否仍是事件所选角色（负责人或创建人）以及钉钉绑定是否有效。没有有效绑定的用户记为跳过；不会根据姓名临时猜测身份或发送给其他员工。
- 发送仍经异步 Outbox 和原有重试流程，钉钉网络请求不在缺陷保存事务中执行。

## 权限与并发

只有管理员可以读取或修改通知规则。规则单独持久化在数据库，保存携带版本号；若另一位管理员先保存，页面保留当前草稿并提示重新加载，防止覆盖他人的配置。

通知规则控制业务事件是否排队，服务器原有钉钉发送开关继续有效。设置页显示“已启用”“演练模式”或“发送暂未启用”；演练模式不会访问真实钉钉消息接口。

## 数据升级

- 新增 `notification_rule_settings` 单行配置表，保存规则、版本号和最后修改人。
- Outbox 新增唯一 `event_key`：新建按缺陷 UUID 去重，状态流转按对应状态活动 UUID 去重。替换旧的 `(event_type, aggregate_id)` 唯一约束，允许同一缺陷多次状态流转。
- 投递记录新增 `recipient_roles` 角色快照，已有投递默认保留负责人角色；Worker 据此支持创建人收件。
- 默认规则只在首次初始化时写入；后续启动不会覆盖管理员保存的配置。

## 验证

```powershell
npm run build
npm test
```

数据库整合测试仅显式启用并断言专用回环数据库，使用模拟消息发送，结束后清理创建的数据：

```powershell
$env:TRACEBUG_NOTIFICATION_TEST = 'true'
npm run test:notifications
Remove-Item Env:TRACEBUG_NOTIFICATION_TEST
```

覆盖规则权限及校验、并发保存、收件人合并、状态重复进入、单条与批量流转、字典名称变化、未绑定账号以及实际 Worker 对创建人的投递。主线字典和原钉钉登录也有回归检查。不会为了页面验收向真实员工发送测试消息。

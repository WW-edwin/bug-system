# 缺陷操作权限

管理员可以修改和删除所有缺陷。普通成员按创建人与负责人关系判断：

| 当前身份 | 修改内容及属性 | 单条及批量修改状态 | 删除缺陷 |
| --- | --- | --- | --- |
| 管理员，无论是否创建或负责 | 可以 | 可以 | 可以 |
| 创建人 | 可以 | 可以 | 可以 |
| 负责人，但不是创建人 | 可以 | 可以 | 不可以 |
| 其他普通成员 | 不可以 | 不可以 | 不可以 |

“修改内容及属性”包含标题、描述、证据附件、状态、优先级、运行环境、模块和负责人。查看与评论沿用原有规则。

后端在事务内锁定缺陷并检查账号 UUID。删除只有在校验通过后才删除缺陷、评论和关联附件；无权请求返回 403。批量修改先检查全部缺陷，有任意无权项时整批拒绝，不产生部分修改。管理员拥有跨缺陷操作权限，但仍需通过登录及首次密码设置，并遵守字典有效值等输入校验。

前端列表、批量选择和详情页使用同一修改判定，删除使用独立判定。无权编辑时，详情字段只读，附件保留预览和下载，不提供删除/上传或保存入口。最终授权由后端决定。

权限整合测试覆盖创建人、仅负责人、无关普通成员和无关管理员，以及未登录/待设密、拒绝删除的内容与附件保护、批量原子性和状态通知。只能在专用隔离数据库中显式启用；运行说明见 `tests/README.md`。

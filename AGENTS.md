# 项目操作规则

## 使用线上数据进行本地自测

当本地前端通过 `VITE_API_PROXY` 连接线上后端时，线上已有数据必须视为只读：不得修改或删除既有用户、项目、缺陷、活动、状态、负责人、评论和附件。

必须遵守以下流程：

1. 自测数据只能在本次测试中新建，并统一使用可追踪标识 `SELFTEST-YYYYMMDD-HHMMSS`。
2. 创建时记录所有自测数据的 ID、关联附件和依赖关系，不得复用或改写线上既有记录。
3. 测试结束后按依赖关系反向删除全部自测数据及附件，并通过刷新、搜索或 API 查询确认零残留。
4. 无法保证完整清理，或测试必须修改既有数据时，立即停止线上数据测试，改用本地快照或专用测试环境。
